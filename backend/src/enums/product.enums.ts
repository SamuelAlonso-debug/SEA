export enum ProductNameLength {
  MIN = 2,
  MAX = 100,
}

export enum ProductCodeLength {
  MIN = 2,
  MAX = 50,
}

export enum ProductCategoryLength {
  MIN = 0,
  MAX = 50,
}

export enum ProductProviderLength {
  MIN = 0,
  MAX = 100,
}

export enum ProductPriceLimits {
  MIN = 0,
  MAX = 1_000_000,
}

export enum ProductStockLimits {
  MIN = 0,
  MAX = 1_000_000,
}
