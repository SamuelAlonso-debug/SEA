export enum UserNameLength {
  MIN = 2,
  MAX = 50,
}

export enum UserLastNameLength {
  MIN = 2,
  MAX = 50,
}

export enum UserEmailLength {
  MIN = 5,
  MAX = 100,
}

export enum UserPasswordLength {
  MIN = 6,
  MAX = 72,
}
